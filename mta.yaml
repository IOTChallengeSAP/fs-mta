ID: fs_mta
_schema-version: '3.1'
version: 0.0.2
modules:
  
  - name: fs-mta-js
    type: nodejs
    path: fs-mta-js
    provides:
      - name: fs-mta-js_api
        properties:
          url: '${default-url}'
    parameters:
      memory: 128M
